name: julelotteri_frontend
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  grpc: ^3.0.0
  protobuf:
  file_picker: ^5.2.2
  internet_connection_checker_plus: ^2.7.1
  sqlite3: ^2.4.7
  excel: ^4.0.6
  sqlite3_flutter_libs: ^0.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
